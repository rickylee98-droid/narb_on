numpy>=1.24
scipy>=1.10
networkx>=3.0
pandas>=2.0

# Optional but recommended: lets the CLI retune BLAS thread counts at runtime
# rather than relying on environment variables set before import.
threadpoolctl>=3.1

# Optional: compiles the periodic overlap kernel, worth ~5x on the Monte Carlo
# search. Without it the tested NumPy fallback runs instead.
numba>=0.59

# Exact symbolic eigenvalues for the amplituhedron flip graph, so that large
# multiplicities are read off a characteristic polynomial rather than a
# floating-point tolerance. The tiling geometry itself needs no symbolic algebra:
# it runs entirely in Python integers.
sympy>=1.12

# Test suite only.
pytest>=7.0
